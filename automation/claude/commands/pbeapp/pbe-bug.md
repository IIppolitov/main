---
description: Разобрать задачу из Яндекс Трекера и найти причину в коде
argument-hint: SUPPORTDEV-963 [SUPPORTDEV-1024 ...] [--links]
allowed-tools: Bash(.claude/scripts/tracker-issue.sh:*), Bash(.claude/scripts/wiki-page.sh:*), Bash(.claude/scripts/db-query.sh:*), Read, Grep, Glob
---

Разбери задачу из Яндекс Трекера: **$ARGUMENTS**

> Это **только исследование**. Код не править, файлы не создавать, ветки не заводить —
> даже если причина очевидна и фикс в одну строку. Закончи разбором и предложением, что чинить,
> и остановись. К реализации переходить только по отдельной команде пользователя.

## 1. Выгрузи задачу

```
bash .claude/scripts/tracker-issue.sh $ARGUMENTS --attach-dir <твой scratchpad>/tracker
```

Аргументы передай как есть: ключей может быть несколько (дубли одного бага, баг и связанная
с ним постановка), и там же может быть флаг `--links`. Подставь свой scratchpad-каталог.
Если его нет — опусти флаг `--attach-dir`: скрипт положит вложения во временный каталог
(по подкаталогу на задачу) и напечатает пути.

Скрипт печатает шапку задачи, кастомные поля очереди, описание, связи, комментарии и список вложений. **Скачанные скриншоты обязательно открой через Read** — в багрепортах половина информации именно там (текст ошибки, экран, состояние UI).

Связи печатаются всегда, но тела связанных задач тянутся только по `--links`. Если флага
не было, а связь выглядит существенной (дубль, «блокирует») — назови её и предложи
перезапуск с `--links`.

Если задач несколько — считай, что это один баг, описанный с разных сторон: своди шаги
воспроизведения и окружение в одну картину, а противоречия между задачами называй явно
и ориентируйся на более свежую.

Если скрипт упал на отсутствии токена (код 3) — покажи его инструкцию по настройке и остановись, не пытайся обойти.

## 1a. Ссылка на регламент в Вики — если есть

В багрепорте может оказаться ссылка на `wiki.yandex.ru`: регламент или спека, где описано
ожидаемое поведение. В отличие от постановки, здесь это **не обязательный** вход — но если
ссылка есть, выгрузи страницу и сверь ожидаемое с фактическим. Без спеки «так и задумано»
не отличить от дефекта, и починка легко превращается в поломку осознанного поведения.

```
bash .claude/scripts/wiki-page.sh <ссылка целиком или slug>
```

Ссылку передавай как есть: схема, домен, `?query` и `#якорь` отбрасываются, `%`-кодировка
раскодируется. Текст приходит в разметке Вики (`{% note %}`, `#| ... |#`) — читай как есть.

Что прочитал, скажи в сверке понимания; если страницу выгрузить не удалось — назови её явно
и разбирай без неё, но с оговоркой, что ожидаемое поведение не подтверждено спекой.

## 1б. Посмотри в базу — только если симптом про данные

Часть багов по коду не разбирается в принципе: «пусто вместо списка», задвоение,
не тот статус, не бьются цифры. Причина там не в ветке кода, а в конкретной строке.

**Когда идти.** Симптом описан как расхождение в данных и в задаче есть, за что
зацепиться: id записи, номер согласия, дата, домен. Идентификатора нет — не иди,
а скажи в сверке понимания, что для проверки данных его не хватает.

**Когда не ходить.** Симптом про вёрстку, про доступ к разделу, про ошибку 500
со стектрейсом, про поведение кнопки. Там ответ в коде, а поход в базу — это
боевые данные в контексте сессии без всякой пользы.

```
# какие базы есть на сервере клиента
bash .claude/scripts/db-query.sh --profile <клиент> \
  -Q "SELECT name FROM sys.databases ORDER BY name"

# где искать внутри базы
bash .claude/scripts/db-query.sh --profile <клиент> -d <база> \
  -Q "SELECT TABLE_SCHEMA, TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME LIKE '%consent%'"

# сама проверка
bash .claude/scripts/db-query.sh --profile <клиент> -d <база> \
  -Q "SELECT TOP 20 Id, StatusId, CreateDate FROM dbo.tPersonConsent WHERE Id = 12345"
```

Обёртка подставляет только сервер, логин и пароль — дальше обычный `sqlcmd`:
флаги и SQL пишешь сам. Пароль в неё не передаётся и в твой контекст не попадает,
ради этого она и существует.

- **`--profile`** обязателен и умолчания не имеет: мнемоника клиента, чей бренд разбирается в задаче (`vlt`, `srv`). Серверов несколько, они
  выглядят одинаково, и запрос, ушедший не в тот, вернёт чужие цифры —
  правдоподобные и не те. Профиля нет — скрипт напечатает, как его завести;
  делает это человек, подключение не выдумывай и пароль в чат не проси.
- **Базу не угадывай.** На сервере клиента их несколько — своя под админку,
  своя под согласия. Имя берётся из настроек подключения клиента; если его нет
  под рукой, спроси у сервера первым запросом (пример выше) и назови в `-d`.
- **Схему тоже не угадывай.** Схемы здесь именованные, и `dbo` часто пустышка:
  на боевом сервере `dbo.tPersonConsent` содержит 0 строк, а реальные данные
  лежат в `consents.tPersonConsent`. Пустая выборка — повод проверить схему,
  а не вывод «данных нет».
- **Имя базы в списке не значит, что доступ есть.** `sys.databases` показывает
  и то, что учётке не открыть. Отказ на `-d` — норма, а не поломка.
- **Флаги `sqlcmd`:** `-W` и `-y` несовместимы, вызов упадёт до подключения.
  Нужен неурезанный вывод длинных строк — `-y 0` без `-W`. Обёртке принадлежат
  только длинные флаги (`--profile`), короткие уходят в `sqlcmd` как есть.

Правила, которые теперь держатся не на скрипте:

- **Только чтение гарантирует учётная запись**, а не проверка текста запроса.
  Она `db_datareader`, записать в базу не выйдет при всём желании. Правка
  данных в PROD идёт задачей и руками: предложить её ты можешь, сделать нет.
- **Персональные данные не запрашивай** — ФИО, телефоны, адреса. Разбор
  делается по идентификаторам: id в задаче есть, ФИО для ответа не нужно.
  Технического предохранителя здесь **нет**, это правило держится на тебе.
  Если без значения ПД вопрос не решается — так и скажи, это смотрит человек.
- **Ограничивай выборку сам.** `TOP`, `COUNT`, `GROUP BY`. Тысячи строк в
  контексте не помогают разбору и стоят денег.

**Что попадает в отчёт: вывод, а не выборка.** «У 412 записей из 15 340 пустой
`RegionId`, все созданы после 12.08» — это факт для разбора. Таблица на 40 строк
с боевыми данными — не факт, а утечка в задачу. Строки не копируй ни в отчёт,
ни в комментарий задачи, ни в файл.

Скрипт может отказать: нет профиля или пароля (код 3), нет `sqlcmd` (4), дальше
идут коды самого `sqlcmd`. Ни один из них — не повод останавливаться: скажи
«данные не проверены, причина такая-то» и иди дальше, пометив выводы, которые
от непроверенных данных зависят.
## 2. Сверка понимания — выведи это ПЕРВЫМ, до похода в код

Прежде чем что-либо искать в репозитории, покажи пользователю, что ты прочитал и как понял.
Это отдельный блок в ответе, не сноска в конце. Цель — чтобы пользователь мог остановить тебя
до того, как ты полчаса копаешь не в ту сторону.

**Что я прочитал:**
- задача (тема, статус, автор, дата), сколько комментариев, какие вложения и что на них
  (по каждому скриншоту — одна строка, что именно на нём видно)
- связанные задачи, если есть
- страницы Вики, если в задаче были ссылки: какие прочитал и какие выгрузить не удалось

**Как я понял проблему:**
- **Что сломано** — одним предложением
- **Шаги воспроизведения** — если их нет, скажи прямо, что их нет
- **Ожидаемое / фактическое поведение**
- **Окружение** — бренд, платформа (web / iOS / Android), версия приложения, роль пользователя.
  Ищи в кастомных полях, тексте и на скриншотах. Чего не нашёл — так и напиши, не додумывай
- **Где, по-моему, копать** — 1–2 предложения, куда пойдёшь смотреть и почему

**В чём не уверен:** список мест, где ты достроил смысл сам. Если такого нет — скажи «всё однозначно».
Если чего-то критично не хватает (нет шагов, нет бренда, видео вместо описания) — скажи об этом
здесь же и предложи уточнить, но работу не останавливай: иди дальше по лучшей версии
и держи её как гипотезу, а не как факт.

Комментарии часто уточняют или отменяют исходное описание — если так, ориентируйся
на последнее по времени и скажи об этом явно.

## 3. Найди причину в коде

Это мультибренд-проект (см. CLAUDE.md), поэтому:

- Если в задаче назван конкретный бренд — начни с `src/app/modules/<код бренда>/`, но проверь и общий код: баг может быть в `services/` / `components/`, а проявляться только у одного бренда из-за подмены провайдера в брендовом модуле.
- Если бренд не назван — считай, что баг общий, и проверь, не ломает ли он остальные бренды.
- Платформозависимые симптомы (только iOS / только Android) — смотри Capacitor-обёртки в `services/` и нативные части.

Не правь код на этом шаге. Задача — дойти до конкретного места и объяснить механизм.

## 4. Отчитайся

Этот отчёт — **технический, для разработчика**: здесь нужны файлы и строки. Дай в ответе:

1. Суть бага (из шага 2)
2. Причину: файл и строки в виде кликабельных ссылок `[file.ts:42](src/file.ts#L42)`, и почему
   код ведёт себя именно так. Если по истории видно, чем и когда внесено (`git log -S`,
   `git blame`) — скажи, и чего не хватило, чтобы поймать раньше (не было теста, не проверялся
   этот бренд или платформа)
3. Затронутые бренды и платформы. Список строй **по коду**, а не по тому, кто прислал багрепорт:
   репортил один клиент, а общий код ломается у всех. Отдельно скажи, задевало ли всех
   пользователей затронутого бренда или только при условии (данные, состояние, офлайн,
   первый запуск)
4. Что чинить — вариант исправления и его риски для других брендов

Если до причины дойти не удалось — скажи это прямо, перечисли что проверил и какой информации не хватает в задаче. Не выдумывай правдоподобную версию.

Последней строкой напомни: по итогу починки в задачу Трекера идёт **нетехнический отчёт для
команды ТП** — «Отчёт по багфиксу» в CLAUDE.md. Разобранное здесь (симптом, причина, охват
брендов и платформ) — сырьё для его блоков 1–3, но в самом отчёте всё это переводится
на язык пользователя, без имён файлов и классов. Сам отчёт сейчас не пиши: фикса ещё нет,
а без него нет блока «как решили».

Дальше, если понадобится: `task-planner` на декомпозицию нетривиального фикса, `feature-developer` на реализацию, `qa-tester` на тесты.
