# Система: pbeconsole — внутренняя консоль

| | |
|---|---|
| **Что это** | Внутренняя CRM/консоль Powbee для ведения дел: клиенты, релизы, мониторинг |
| **Потребитель** | Сотрудники Powbee (внутренний продукт) |
| **Репозиторий** | `/Users/amlung/Documents/Powbee/pbeconsole` |
| **Источник истины по коду** | [pbeconsole/CLAUDE.md](../../../pbeconsole/CLAUDE.md) |
| **Владелец от бизнеса** | TODO |
| **Ведущий от ИТ** | Ипполитов Иван — разработку ведёт лично, отдел не закреплён |
| **Режим поддержки** | Без обязательств — внутренняя разработка |
| **Очередь Трекера** | `CONSOLE` — вне регламента жизненного цикла |

## Назначение

Учёт того, что мы поставляем клиентам. Сущность `Client` = фармкомпания-заказчик;
продукт для неё состоит из двух частей — **Application** ([pbeapp](pbeapp.md)) и
**Admin** ([pbeadmin](pbeadmin.md)). Релизы (`ApplicationRelease`) ведутся по
одному на клиента.

Это единственная из трёх систем, где сходится картина «какому клиенту что и
когда поставлено» — то есть основной источник управленческой отчётности.

## Стек

| Слой | Технологии |
|---|---|
| Backend | PHP 8.2, Laravel 13 |
| БД (прод) | MSSQL (`sqlsrv`), доменные схемы, паттерн «Fields» |
| БД (тесты) | SQLite `:memory:` |
| UI | Tailwind CSS 4 + Vite, Blade, собственная дизайн-система (не Bootstrap/AdminLTE) |
| Интерактив | Alpine.js, Tom Select, ApexCharts (опт-ин) |
| Тесты | PHPUnit 12, Pint |
| Auth | Кастомный `AuthController` (логин `Login`, хэш `PasswordHash`) |

## Доменные области

| Схема БД | Что ведёт |
|---|---|
| `client` | Клиенты, файлы клиентов, инстансы админки, снапшоты |
| `release` | Релизы приложения (стадии, версии, возвраты) и **релизы админки** — карточка релиза со списком проектов Трекера, добором и снапшотом состава |
| `access` | Пользователи, роли, права |
| `integration` | Токены трекера, чаты мессенджеров |
| `monitoring` | Цели алертов, ссылки на wallboard |
| `notification` | Уведомления и сообщения |

## Интеграции

Yandex Tracker (`YandexTrackerService`), почтовые уведомления, мессенджеры,
мониторинг с алертами.

## Управленческая ценность

- Прозрачность релизного цикла по каждому клиенту.
- **Единственное место, где объявлен состав релиза админки.** Карточка релиза
  связывает номер релиза, версию продукта (`v7.0`) и пару спринтов, держит проект
  добора «Вне спринта» и сводит по ним задачи из Трекера — на это опирается
  [этап 13](../regulations/crm-lifecycle/13-reliz.md) жизненного цикла.
- Данные для отчётности руководству и заказчикам.
- Мониторинг — точка раннего предупреждения по инцидентам.

## Открытые вопросы

- TODO: закреплён ли регламент ведения релизов, или заполнение держится на людях.
- TODO: полнота данных — используется ли консоль всеми командами или частью.
- Тесты идут на SQLite, прод на MSSQL — расхождение диалектов маскирует часть ошибок.
