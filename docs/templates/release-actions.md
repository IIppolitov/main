# Release actions по клиентам

Релиз от <ДД.ММ.ГГГГ>. Источник — проекты Трекера <номера>, <N> задач.
**База сверки:** тег `<prod-vX.Y>` (продуктив на <ДД.ММ.ГГГГ>)<, кандидат `pre-prod-vX.Y`>.
Проверено на нём: расположение конфиг-ключей, миграции, сидеры, указатели сабмодулей.
<Или: сверка по коду не проводилась — доступа к репозиториям нет, факты из подзадач «Релиз».>

**Легенда:** ✅ в теге сверки · 🟡 влито в `main`, но в тег не входит · 🔴 блокер релиза ·
⚠️ требует внимания при выполнении

---

## <Клиент>

Задачи релиза: <CRM-…, CRM-…>.

### Миграции

- [ ] `<полное_имя_файла_миграции>` — <CRM-…>, <что создаёт; соединение ядра или модуля>

### Конфиги

Ключи настроек админки (`config/pbe.php`, значения ставятся в настройках инстанса):

- [ ] `<KEY>` = <значение> — <CRM-…> (<дефолт в коде>, <что включает>)

env-ключи (`.env` инстанса, ставит CTO):

- [ ] `<KEY>=<значение>` — <CRM-…>

### Ручная настройка

- [ ] **<CRM-…>** — <что создать/запустить руками, по инструкции из тела задачи>

### Раскатка по инстансам

- [ ] **<РФ / КЗ / УЗ>** — <CRM-…> (<что именно>)

### Без действий релиза

- **<CRM-…>** — <почему ничего делать не нужно: схема и конфиги не менялись>

> ⚠️ <Строгий порядок шагов, зависимость от планировщика, ловушка с env в
> миграции — всё, что ломает выкатку, если сделать не в том порядке.>

---

## Сводка по бампу указателей сабмодулей

| Модуль | Задачи | Указатель в `<prod-vX.Y>` | Статус |
|---|---|---|---|
| <Module> | <CRM-…> | <содержит / не содержит> | <✅ бамп не нужен / 🟡 нужен бамп> |

> Статус берётся из сравнения указателя сабмодуля в теге с коммитом задачи, а не
> из того, влита ли задача в `main` модуля.
>
> <Где есть миграции модуля, а где релизные задачи схему не меняют.>

---

## Открытые вопросы до релиза

1. **<CRM-…>** — <чего не хватает и у кого спросить>.

---

## Быстрый чек-лист по инстансам

| Клиент | Миграции | Конфиги/env | Ручные шаги |
|---|---|---|---|
| <Клиент> | <файл или —> | <ключи> | <что руками> |
